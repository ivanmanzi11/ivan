// List of Student objects
class Student {
  String name;
  int age;

  Student(this.name, this.age);
}

void main() {
  List<Student> students = [
    Student("bertin", 20),
    Student("Bwiza", 22),
    Student("John", 19),
  ];

  // No output
}
